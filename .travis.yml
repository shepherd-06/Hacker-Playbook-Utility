language: python
os: linux
sudo: required
python:
  - "3.4"
  - "3.5"
  - "3.6"

#virtualenv:
#    system_site_package: true

before_install:
 - sudo apt-get update

install:
 - pip3 install grin
 - pip3 install distro
 - pip3 install peppercorn
 - pip3 install setuptools
 - pip3 install wheel
 - pip3 install argparse

script:
  - sudo python3 -m unittest -v test_utility.py

notifications:
  email:
     on_success: change
     on_failure: always